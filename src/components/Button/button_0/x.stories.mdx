import { C0Button, M0Button } from '.'

import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
} from '@storybook/addon-docs'

export const Template = (args) => (
  <C0Button {...args} />
)

<Meta
  title='Components/Button/0'
  component={C0Button}
/>

# Botao

## Dependencias 🛠

### **lib**

### **Components**

## Propriedades 📐

```ts
type Settings = {
  children?: ReactNode
  disabled?: boolean
  onClick?: (
    e: MouseEvent<HTMLButtonElement>
  ) => any
  icon?: ReactNode
  ref?: LegacyRef<HTMLButtonElement>
  custom?: buttonCustom
  outline?: boolean
  iconDirection?: 'left' | 'right'
  meta?: ButtonHTMLAttributes<HTMLButtonElement>
}

export type buttonCustom = {
  enabled: {
    color: string
    bg: string
  }
  disabled: {
    color: string
    bg: string
  }
}

export type buttonProps = {
  loadingSettings?: Settings
  isLoading?: boolean
} & Settings
```

## Exemplo 📌

<Canvas>
  <Story name='Default' args={M0Button.default}>
    {Template.bind({})}
  </Story>
</Canvas>
